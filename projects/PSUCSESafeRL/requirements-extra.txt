# Delta packages for PSUCSESafeRL, converted from that repo's environment.yml.
#
# Deliberately EXCLUDED because Lambda Stack images already ship them (this
# venv is created with --system-site-packages, so it inherits these instead
# of paying to store/reinstall duplicates):
#   torch, torchvision, triton, numpy, sympy, mpmath, networkx, nvidia-*-cu12, cuda-*
#
# safety-gymnasium is deliberately NOT listed here: this project's own
# Readme.md notes the PyPI release (v1.0.0) is outdated and v1.2.0 must be
# installed from source. See setup-extra.sh, which handles that step.
#
# Versions pinned to what environment.yml recorded, as a starting point —
# if Lambda Stack's torch/numpy differ meaningfully from what this project
# was validated against (torch==2.10.0, numpy==1.23.5), some of these may
# need loosening. bootstrap.sh's version-drift warning will flag if the
# instance's Python differs from the one the venv was built against.

absl-py==2.4.0
ale-py==0.11.2
annotated-doc==0.0.4
annotated-types==0.7.0
beautifulsoup4==4.14.3
click==8.3.1
cloudpickle==3.1.2
contourpy==1.3.2
cycler==0.12.1
farama-notifications==0.0.4
filelock==3.25.0
fonttools==4.61.1
fsspec==2026.2.0
gdown==5.2.1
gitdb==4.0.12
gitpython==3.1.46
glfw==2.10.0
grpcio==1.78.0
gymnasium==0.28.1
gymnasium-robotics==1.2.2
imageio==2.37.2
imageio-ffmpeg==0.6.0
jax-jumpy==1.0.0
jinja2==3.1.6
kiwisolver==1.4.9
markdown==3.10.2
markdown-it-py==4.0.0
markupsafe==3.0.3
matplotlib==3.10.8
mdurl==0.1.2
moviepy<2.2  # 2.2.1 pins numpy>=1.25, which conflicts with gymnasium-robotics's numpy<1.24 — let pip pick a compatible version
mujoco==2.3.3
omnisafe==0.5.0
opencv-python==4.8.0.74
pandas==2.0.3
pettingzoo==1.24.3
pillow==11.3.0
proglog==0.1.12
protobuf==6.33.5
pydantic==2.12.5
pydantic-core==2.41.5
pygame==2.1.0
pyopengl==3.1.10
pyparsing==3.3.2
python-dotenv==1.2.2
pytz==2025.2
pyyaml==6.0.3
rich==14.3.3
seaborn==0.13.2
sentry-sdk==2.54.0
shellingham==1.5.4
smmap==5.0.2
soupsieve==2.8.3
stable-baselines3==2.1.0
tensorboard==2.20.0
tensorboard-data-server==0.7.2
tqdm==4.67.3
typer==0.24.1
typing-inspection==0.4.2
tzdata==2025.3
wandb==0.25.0
werkzeug==3.1.6
xmltodict==1.0.4
